// variables and values
let firstName = "jonas";
console.log(firstName);

var myName = " Reuben ";
let age = 23;

console.log(" my name is " + myName + " I'm " + age + "years ");

/*let, const  ,var
    let is used when the variable can change 
the const is used when the variable will not changed  and when using we also need an  initializer.
var is also used to declare variables that  can be change
*/
